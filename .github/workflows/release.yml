name: Release

on:
  push:
    branches: [main]
    paths:
      - 'packages/cli/**'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  # id-token is forwarded to the publish job (needed for npm OIDC publishing)
  id-token: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.check-release.outputs.release_created }}

    steps:
      - name: Run release-please
        uses: googleapis/release-please-action@v4
        id: release
        with:
          release-type: node
          path: packages/cli

      # When release-please creates a PR (but no release yet), auto-merge it
      # and re-run release-please to create the actual release.
      # This is needed because GITHUB_TOKEN events don't trigger other workflows,
      # so a separate auto-merge workflow cannot be triggered by release-please PRs.
      - name: Auto-merge release PR
        if: steps.release.outputs.prs_created == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          PR_NUMBER=$(echo '${{ steps.release.outputs.prs }}' | jq -r '.[0].number')
          echo "Merging release PR #${PR_NUMBER}"
          gh pr merge "$PR_NUMBER" --squash --repo "${{ github.repository }}"

      - name: Checkout updated main
        if: steps.release.outputs.prs_created == 'true'
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Create release after merge
        if: steps.release.outputs.prs_created == 'true'
        uses: googleapis/release-please-action@v4
        id: release-after-merge
        with:
          release-type: node
          path: packages/cli

      - name: Check if release was created
        id: check-release
        run: |
          RELEASED="false"
          if [ "${{ steps.release.outputs.release_created }}" = "true" ] || \
             [ "${{ steps.release.outputs.releases_created }}" = "true" ] || \
             [ "${{ steps.release-after-merge.outputs.release_created }}" = "true" ] || \
             [ "${{ steps.release-after-merge.outputs.releases_created }}" = "true" ]; then
            RELEASED="true"
          fi
          echo "release_created=${RELEASED}" >> "$GITHUB_OUTPUT"
          echo "Release created: ${RELEASED}"

  publish:
    name: Publish
    needs: release
    # Publish when release-please created a release, or when triggered manually
    if: ${{ needs.release.outputs.release_created == 'true' || github.event_name == 'workflow_dispatch' }}
    uses: ./.github/workflows/publish.yml
    permissions:
      contents: read
      id-token: write
