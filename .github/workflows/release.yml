name: Release

on:
  push:
    branches: [main]
    paths:
      - 'packages/cli/**'

permissions:
  contents: write
  pull-requests: write
  # id-token is forwarded to the publish job (needed for npm OIDC publishing)
  id-token: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}

    steps:
      - name: Run release-please
        uses: googleapis/release-please-action@v4
        id: release
        with:
          release-type: node
          path: packages/cli
          merge: true

  publish:
    name: Publish
    needs: release
    # Only publish when release-please actually created a new GitHub Release
    if: ${{ needs.release.outputs.release_created == 'true' }}
    uses: ./.github/workflows/publish.yml
    permissions:
      contents: read
      id-token: write
